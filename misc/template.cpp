#include <bits/stdc++.h>
using namespace std;

#define sz(c) int(c.size())
#define all(c) c.begin(), c.end()

int main() {
    ios::sync_with_stdio(false);
#ifndef LOCAL
    cin.tie(nullptr);
#endif
    
}
